import React from "react";

const Single = () => {
  return (
    <section className="single-section">
      <div className="single-project">
        <h2>SnackLab</h2>
        <p>
          SnackLab is a web application that allows users to search for recipes
          based on ingredients they already have in their pantry. The app
          utilizes the Edamam API to search for recipes based on user input.
        </p>
      </div>
    </section>
  );
};

export default Single;
